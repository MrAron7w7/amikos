import 'package:flutter/material.dart';

class AppStyle {
  // Estilo del texto
  static TextStyle textStyleFont({
    double? fontSize,
    FontWeight? fontWeight,
    Color? color,
  }) {
    return const TextStyle();
  }
}
